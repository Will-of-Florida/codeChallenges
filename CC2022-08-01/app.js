// Modify the spacify function so that it returns the given string with spaces inserted between each character.
function spacify(str) {
    return str.split('').join(' '); //split('')splits the string into array of letters. Join(' ') joins the array into a string with a space between each letter
  }
